services:
  stdfolio:
    container_name: stdfolio
    build: .
    restart: unless-stopped
    expose:
      - "4173"
    environment:
      NODE_ENV: production
    networks:
      - proxy

networks:
  proxy:
    external: true
